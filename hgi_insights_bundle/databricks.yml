# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "hgi_insights_bundle"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-d12eb73f-6dae.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-d12eb73f-6dae.cloud.databricks.com
      # We always use /Users/ayush.n.gunjal@v4c.ai for all resources to make sure we only have a single copy.
      root_path: /Users/ayush.n.gunjal@v4c.ai/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as ayush.n.gunjal@v4c.ai in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: ayush.n.gunjal@v4c.ai
